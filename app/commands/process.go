package commands

// Here it needs to go all the processing of the commands and only be called in connection. Why this?
// This way I can just call whatever I need to call in the right process. I think I was kind of doing it, but
// I feel I was going in the wrong direction. Think more in a declarative programming style, more than OOP
//
// Example:
//
// func processString() {
//
// }
//
// func addToMemory(key, value) {
//
// }
//
// func getFromMemory(key, value) {}
//
// func processStatusCommand() {}
